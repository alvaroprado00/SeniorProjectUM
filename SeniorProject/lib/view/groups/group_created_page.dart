import 'package:flutter/material.dart';

class GroupCreated extends StatelessWidget {
  const GroupCreated({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Group Created')),
    );
  }
}
